{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vocoding.net/schemas/command-directive.schema.json",
  "title": "CommandDirective",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "command": { "type": "string" },
    "args": {
      "type": "array",
      "items": { "type": "string" }
    },
    "workingDirectory": {
      "type": "string",
      "description": "Optional absolute directory passed to spawn cwd; when set, the child process starts in this folder."
    },
    "timeoutMs": { "type": "integer" },
    "detached": {
      "type": "boolean",
      "description": "When true, the host opens an editor integrated terminal for the command (user sees logs, Ctrl+C stops). Does not wait for exit. timeoutMs is ignored."
    }
  },
  "required": ["command"]
}
